name: agent_harness
description: Core framework for AI Agent CLI commands, state persistence, and stdout/JSON output.
version: 1.0.0

environment:
  sdk: ">=3.3.0 <4.0.0"

dependencies:
  path: ^1.9.0

dev_dependencies:
  test: ^1.24.0
  lints: ^3.0.0

