name: agent_backlog
description: Standalone task backlog & spec manager for loading, parsing, and updating Markdown checklists and YAML backlogs.
version: 1.0.0

environment:
  sdk: ">=3.3.0 <4.0.0"

dependencies:
  yaml: ^3.1.2

dev_dependencies:
  test: ^1.24.0
  lints: ^3.0.0
